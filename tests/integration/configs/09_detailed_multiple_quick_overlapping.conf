# Scenario 9: Single detailed + multiple quick jobs on overlapping routers (partial queueing)
# Format: trace_file ; analysis_mode ; dest_ports

# Detailed job: Uses routers A, B, C
/path/to/traces/trace_routerA_routerB_routerC.json ; detailed ; 80,443

# Quick job 1: Uses routers B, C, D (BLOCKED - overlaps with detailed)
/path/to/traces/trace_routerB_routerC_routerD.json ; quick ; 22,8080

# Quick job 2: Uses routers X, Y (OK - no overlap)
/path/to/traces/trace_routerX_routerY.json ; quick ; 3306,5432

# Quick job 3: Uses routers C, D, E (BLOCKED - overlaps with detailed)
/path/to/traces/trace_routerC_routerD_routerE.json ; quick ; 6379,27017
