# Test configuration for traceroute simulator
# Relative path from tests/ directory where test suite runs
routing_dir: "routing_facts"
verbose: true
verbose_level: 2
json_output: false
enable_mtr_fallback: false  # Disable MTR for testing
enable_reverse_trace: false
# controller_ip: "192.168.1.100"  # Required for reverse path tracing