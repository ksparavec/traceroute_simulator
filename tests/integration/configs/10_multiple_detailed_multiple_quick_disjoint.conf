# Scenario 10: Multiple detailed jobs + multiple quick jobs on disjoint routers
# Format: trace_file ; analysis_mode ; dest_ports

# Detailed job 1: Uses routers A, B, C
/path/to/traces/trace_routerA_routerB_routerC.json ; detailed ; 80,443

# Detailed job 2: Uses routers X, Y, Z (no overlap with Detailed 1)
/path/to/traces/trace_routerX_routerY_routerZ.json ; detailed ; 80,443

# Quick job 1: Uses routers P, Q (no overlap)
/path/to/traces/trace_routerP_routerQ.json ; quick ; 22,8080

# Quick job 2: Uses routers W, V (no overlap)
/path/to/traces/trace_routerW_routerV.json ; quick ; 3306,5432

# Quick job 3: Uses routers S, T (no overlap)
/path/to/traces/trace_routerS_routerT.json ; quick ; 6379,27017
