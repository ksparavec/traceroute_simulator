# Scenario 3: Multiple detailed jobs with overlapping routers
# Format: trace_file ; analysis_mode ; dest_ports

# Job 1: Uses routers A, B, C
/path/to/traces/trace_routerA_routerB_routerC.json ; detailed ; 80,443

# Job 2: Uses routers B, C, D (overlaps with Job 1 on routers B and C)
/path/to/traces/trace_routerB_routerC_routerD.json ; detailed ; 80,443

# Job 3: Uses routers C, D, E (overlaps with Job 2 on routers C and D)
/path/to/traces/trace_routerC_routerD_routerE.json ; detailed ; 22,8080
