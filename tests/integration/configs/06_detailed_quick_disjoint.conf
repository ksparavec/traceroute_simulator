# Scenario 6: Single detailed + single quick on disjoint routers (can run in parallel)
# Format: trace_file ; analysis_mode ; dest_ports

# Detailed job: Uses routers A, B, C
/path/to/traces/trace_routerA_routerB_routerC.json ; detailed ; 80,443

# Quick job: Uses routers X, Y, Z (no overlap with detailed job)
/path/to/traces/trace_routerX_routerY_routerZ.json ; quick ; 22,8080
