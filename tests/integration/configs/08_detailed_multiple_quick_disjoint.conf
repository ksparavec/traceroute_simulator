# Scenario 8: Single detailed + multiple quick jobs on disjoint routers (can run in parallel)
# Format: trace_file ; analysis_mode ; dest_ports

# Detailed job: Uses routers A, B, C
/path/to/traces/trace_routerA_routerB_routerC.json ; detailed ; 80,443

# Quick job 1: Uses routers X, Y (no overlap)
/path/to/traces/trace_routerX_routerY.json ; quick ; 22,8080

# Quick job 2: Uses routers P, Q (no overlap)
/path/to/traces/trace_routerP_routerQ.json ; quick ; 3306,5432

# Quick job 3: Uses routers W, Z (no overlap)
/path/to/traces/trace_routerW_routerZ.json ; quick ; 6379,27017
