# Scenario 2: Multiple detailed jobs on disjoint routers (no overlap)
# Format: trace_file ; analysis_mode ; dest_ports

# Job 1: Uses routers A, B, C
/path/to/traces/trace_routerA_routerB_routerC.json ; detailed ; 80,443

# Job 2: Uses routers X, Y, Z (no overlap with Job 1)
/path/to/traces/trace_routerX_routerY_routerZ.json ; detailed ; 80,443

# Job 3: Uses routers P, Q (no overlap with Job 1 or 2)
/path/to/traces/trace_routerP_routerQ.json ; detailed ; 22,8080
